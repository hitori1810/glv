$(document).ready(function() {
    CKEDITOR.replace( 'common_text', {
        width: 800,
        height: 100,
        resize_dir: 'both',
        resize_minWidth: 200,
        resize_maxHeight: 300,
        resize_maxWidth: 800
    } );
    CKEDITOR.replace( 'detail_text', {
        width: 800,
        height: 100,
        resize_dir: 'both',
        resize_minWidth: 200,
        resize_maxHeight: 300,
        resize_maxWidth: 800
    } );
    CKEDITOR.replace( 'prefer_product_text', {
        width: 800,
        height: 100,
        resize_dir: 'both',
        resize_minWidth: 200,
        resize_maxHeight: 300,
        resize_maxWidth: 800
    } );
});